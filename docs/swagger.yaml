definitions:
  response.HTTPServerResponse:
    properties:
      code:
        type: integer
      data: {}
      message: {}
    type: object
  v1.DemoInfo:
    properties:
      createdAt:
        description: 创建时间
        type: integer
      id:
        type: integer
      message:
        description: 消息
        type: string
      updatedAt:
        description: 更新时间
        type: integer
    type: object
info:
  contact: {}
paths:
  /api/v1/hello:
    get:
      consumes:
      - application/json
      parameters:
      - description: message
        in: query
        name: msg
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/response.HTTPServerResponse'
            - properties:
                code:
                  type: integer
                data:
                  $ref: '#/definitions/v1.DemoInfo'
              type: object
        "500":
          description: 服务器内部错误
          schema:
            type: object
      tags:
      - Demo
swagger: "2.0"
